#include <stdio.h>
#include<conio.h>
void main()
{
    int s, i, sum = 0;
    printf("Enter a positive integer: ");
    scanf("%d",&s);
    for(i=1; i <= n; ++i)
    {
        sum += i;
    }

    printf("Sum = %d",sum);

    return 0;
}
